import React from 'react';
import '../styles/Topbar.scss';

function Topbar(){
    return(
        <div className="topbar">
            <img src="logo.svg" alt="Seidlserver"/>
        </div>
    );
}

export default Topbar;
